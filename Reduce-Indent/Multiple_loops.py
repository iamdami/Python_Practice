# Reduce indents with quotient and remainder

for i in range(N):
  for j in range(N):
    for k in range(N):
      # i, j, k process
      
      
for num in range(N ** 3):
  i, j, k = num // (N * N), num // N % N, num % N
  # i, j, k process
